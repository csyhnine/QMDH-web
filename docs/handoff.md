# Handoff

## Usage Rules

- Keep only the latest 3 handoff entries here.
- Move older records into `docs/archive/` if they still matter.
- Write for a cold-start agent.
- Mark `WIP` explicitly if the repo is not in a safe handoff state.

---

## Latest Handoffs

### [2026-06-09 Final WIP Review/Staging Prep] WIP
- Role: Stop further Studio splitting and prepare `prod-001` for final WIP review/staging.
- Branch: `codex/prod-001-studio-refactor`
- Current local HEAD: `005e25d`
- Repo status:
  - Working tree clean: No
  - No commit, push, or deploy was performed.
  - The WIP remains a broad behavior-preserving Studio refactor.
  - Added `.gitignore` coverage for root `storage/` and `tmp/` so local media/browser/runtime data no longer appears in normal staging candidates.
  - `frontend/dist/`, `storage/`, and `tmp/` now show as ignored in `git status --ignored`.
- Final review fix package completed on 2026-06-09:
  - Fixed Studio reference-upload preview blob cleanup in `useStudioReferenceUploadState.ts`; preview URLs are now cleaned through explicit remove/clear/replace paths and an unmount-only latest-list cleanup.
  - Removed plaintext provider API key return from provider-profile responses; backend schema/router, frontend API type, and provider-profile tests now assert only masked/has-key fields are exposed.
  - Hardened inspiration URL fetching against SSRF: `extract-images` validates initial and redirect URLs, caps HTML size, and preserves explicit HTTP errors; remote inspiration image localization validates public HTTP(S) URLs before download, validates redirects, and caps image bytes.
  - Fixed Redis enqueue failure handling: `/tasks` and Agent image jobs now mark the persisted task/job failed with `error_stage=task_enqueue`, write usage ledger for the failed task, and return HTTP 503 instead of leaving pending/accepted orphan records. `enqueue_task` now closes the Redis client.
  - Hardened production bootstrap admin defaults: non-SQLite production requires `QMDH_BOOTSTRAP_ADMIN_PASSWORD` when bootstrap admin is enabled and rejects the development default; `docker-compose.yml` no longer defaults it to `dev-admin-password`.
  - Fixed partial legacy-DB schema bootstrap handling so `ensure_schema` only alters/indexes tables that exist.
  - Fixed Chat streaming POST to use the shared frontend API base/auth helpers (`buildApiUrl`, `getAuthHeaders`) instead of hardcoded `/api/v1` and bearer-only auth.
- Final prep checks completed:
  - Recounted Studio hotspots from the current worktree: largest TSX module is still `StudioWorkspacePane.tsx` at 52 lines; largest TS files are `useSharedTemplateBrowser.ts` / `studioDerivedState.ts` at 107 lines, `useStudioGalleryActions.ts` / `useSharedTemplatePreview.ts` at 106 lines, and `useStudioReferenceUploads.ts` / `useStudioTemplates.ts` at 103 lines.
  - Backend tests passed: `cd backend; .venv\Scripts\python.exe -m pytest tests -q` => 110 passed, 1 warning, 22 subtests passed.
  - `npm run build` passed.
  - `npm run smoke:studio` passed 8/8 against `http://127.0.0.1:18080`.
  - Final review found and fixed two Studio type-contract issues in `useStudioGalleryActions.ts` and `useStudioTaskSubmission.ts`; `frontend\node_modules\.bin\tsc.cmd --noEmit -p frontend\tsconfig.json` now fails only on the known global type issues (`ImportMeta.env`, PNG module declarations, `DashboardPage.tsx` group summary typing, and `router.tsx` JSX namespace).
  - `git diff --check` passed with only known CRLF warnings.
  - `git add -n .` dry-run did not include forbidden paths: `storage/`, `tmp/`, `.env`, `backend/app.db`, `frontend/dist/`, or `node_modules/`.
  - Risk scan for `debugger`, `TODO`, `FIXME`, and accidental `console.log` found only expected smoke-script logging.
- Candidate staging boundary:
  - Include backend fix files and tests: `backend/app/core/config.py`, `backend/app/routers/agent.py`, `backend/app/routers/inspiration.py`, `backend/app/routers/providers.py`, `backend/app/routers/tasks.py`, `backend/app/schemas.py`, `backend/app/services/bootstrap.py`, `backend/app/services/inspiration_media.py`, `backend/app/services/task_executor.py`, `backend/app/services/url_safety.py`, and the touched backend tests.
  - Include `.gitignore`, docs updates/archive files, `package.json`, `frontend/vite.config.ts`, `frontend/src/components/shared/AuthGuard.tsx`, all current `frontend/src/features/studio/*` refactor files, and `scripts/smoke-studio.mjs`.
  - Continue excluding local-only/runtime/generated paths: `storage/`, `tmp/`, `.env`, `backend/app.db`, `frontend/dist/`, and `node_modules/`.
- Remaining risk:
  - This is still a very wide WIP with a large dry-run staging set.
  - `frontend\node_modules\.bin\tsc.cmd --noEmit -p frontend\tsconfig.json` still fails on known global type debt: `ImportMeta.env`, PNG module declarations, `DashboardPage.tsx` group summary typing, and `router.tsx` JSX namespace.
  - Manual/stronger visual validation is still recommended for shared-template hover preview before commit/deploy.
  - Server/deploy state was not rechecked in this prep step; recheck server HEAD and health before any release work.
- Safe next step:
  - Do not split more for this WIP.
  - Perform final human/code review of the dry-run candidate set, then stage intentionally only after user confirmation.
  - Before commit, rerun backend tests, `npm run build`, `npm run smoke:studio`, and `git diff --check`; before any release, separately recheck server/GitHub state.

### [2026-06-09 00:21] WIP Phase 1/2 Checkpoint
- Role: Studio hotspot refactor, continuing `prod-001`
- Branch: `codex/prod-001-studio-refactor`
- Repo status:
  - Working tree clean: No
  - Current local HEAD: `005e25d`
  - `origin/main` rechecked at `005e25d33e3a99ba4501c46428d19fee522ab91a`
  - A local protection branch was created from `main`; no commit, push, or deploy was performed
  - Tracked changes remain broad Studio/doc WIP: `docs/continuity.md`, `docs/handoff.md`, `docs/tasks.md`, `frontend/src/components/shared/AuthGuard.tsx`, `GenerateStudioShell.tsx`, `StudioComposerDock.tsx`, `StudioHistoryPane.tsx`, `StudioWorkspacePane.tsx`, `frontend/vite.config.ts`, and `package.json`
  - Untracked WIP remains concentrated in `frontend/src/features/studio/*`, plus `scripts/`
  - Local-only residuals still expected and must not be staged: `storage/`, `tmp/`
- Phase 1 completed:
  - Re-ran the required takeover checks: `git status --short`, `git branch --show-current`, `git rev-parse --short HEAD`, and `git log -5 --oneline`
  - Rechecked GitHub `origin/main`; it still points at `005e25d`
  - Created and switched to `codex/prod-001-studio-refactor` to protect the broad WIP away from direct `main` work
  - Recounted Studio hotspots from the current worktree
- Five-stage continuation Stage 1 recheck completed at 2026-06-09 01:31 +08:00:
  - Branch remains `codex/prod-001-studio-refactor`; local HEAD remains `005e25d`
  - Recent commit chain remains `005e25d`, `6ae35b1`, `ceda88e`, `c9a9161`, `57d134c`
  - Working tree remains WIP and uncommitted with tracked changes in `docs/continuity.md`, `docs/handoff.md`, `docs/tasks.md`, `frontend/src/components/shared/AuthGuard.tsx`, `GenerateStudioShell.tsx`, `StudioComposerDock.tsx`, `StudioHistoryPane.tsx`, `StudioWorkspacePane.tsx`, `frontend/vite.config.ts`, and `package.json`
  - Untracked WIP remains concentrated in `frontend/src/features/studio/*` plus `scripts/`; local-only `storage/` and `tmp/` are still present and must not be staged
  - Recounted hotspots from the current worktree: largest TSX modules are `StudioWorkspacePane.tsx` 52 lines, `StudioHistoryFeedItem.tsx` / `StudioSharedTemplatePreviewImages.tsx` / `StudioShareConfirmLightbox.tsx` 49 lines, `StudioComposerCollapsedBar.tsx` 47 lines, then several 46-line shells
  - Largest TS modules are `useGenerateStudioController.ts` 155 lines, `useStudioReferenceUploads.ts` 133 lines, `studioTaskActionUtils.ts` 133 lines, `studioReferenceUtils.ts` 128 lines, `useCustomStudioTemplates.ts` 125 lines, `useStudioTaskSubmission.ts` 122 lines, `useSharedTemplateBrowser.ts` 120 lines, `studioControllerProps.ts` 120 lines, and `useStudioTaskActions.ts` 116 lines
  - Next action in the five-stage loop: Stage 2 should be a safe browser visual smoke pass before more splitting, or one very small controller/helper split with React hook order untouched
- Phase 2 first chunk completed:
  - Split the reference upload transport loop out of `useStudioReferenceUploads.ts` into `uploadReferenceFiles` in `studioReferenceUtils.ts`
  - Kept the existing base64 upload behavior, `api.uploadReferenceImage` call shape, per-file size validation, object URL preview creation, and failure-time blob preview cleanup
  - `useStudioReferenceUploads.ts` now stays focused on React state, form synchronization, input/drop handlers, and submission tracker state
- Phase 2 second chunk completed:
  - Split custom template save/delete API orchestration into new `customStudioTemplateActions.ts`
  - Kept private template validation, create/update/delete API calls, success/error feedback messages, edit-state reset behavior, and sorted list upsert/remove behavior unchanged
  - `useCustomStudioTemplates.ts` now stays focused on draft/edit React state and applying action results to state
- Phase 2 third chunk completed:
  - Split `studioCanvasProps.ts` into focused `studioComposerCanvasPropsBuilder.ts` and `studioHistoryCanvasPropsBuilder.ts`
  - Kept the public `buildStudioCanvasProps` entrypoint unchanged for `StudioDesignerView`
  - Kept composer/history prop wiring behavior unchanged while reducing `studioCanvasProps.ts` to a small composition shim
- Phase 2 fourth chunk completed:
  - Split task submission helpers into `studioTaskSubmissionActions.ts`
  - Kept submit behavior unchanged: provider validation, requested-provider correction, in-flight guard, `api.createTask` payload shape, pending/created submission tracker stages, forced data reload, and failure feedback remain wired through `useStudioTaskSubmission.ts`
  - `useStudioTaskSubmission.ts` now stays focused on React callback orchestration and delegates payload/tracker/shared-template tracking helpers
- Phase 2 fifth chunk completed:
  - Split gallery action state derivation into `studioGalleryActionUtils.ts`
  - Kept gallery behavior unchanged: bookmark/share/delete API calls, pending-action state, feedback messages, share confirmation gating, asset replacement, gallery preview replacement, task removal, and error propagation remain wired through `useStudioGalleryActions.ts`
  - `useStudioGalleryActions.ts` now stays focused on async action orchestration and share-confirm modal state
- Phase 2 sixth chunk completed:
  - Split shared-template browser state derivation into `sharedTemplateBrowserState.ts`
  - Kept template browser behavior unchanged: category auto-expand, invalid category/subcategory reset, active heading labels, quick-filter reset behavior, category/subcategory activation, template filtering, hover preview wiring, impression tracking, and apply tracking remain wired through `useSharedTemplateBrowser.ts`
  - `useSharedTemplateBrowser.ts` now stays focused on React state/effects and template browser event orchestration
- Phase 2 seventh chunk completed:
  - Split composer expanded-content prop partitioning into `studioComposerExpandedContentPropBuilders.ts` and `studioComposerToolbarPropsBuilder.ts`
  - Kept the public `getStudioComposerExpandedContentProps` entrypoint unchanged for `StudioComposerExpandedContent`
  - Kept leading/body/reference-input/toolbar prop shapes unchanged while reducing `studioComposerExpandedContentProps.ts` to a small composition shim
- Phase 2 eighth chunk completed:
  - Split task display/failure/reference/progress helpers into `studioTaskTitleUtils.ts`, `studioTaskFailureUtils.ts`, `studioTaskReferenceUtils.ts`, and `studioTaskProgressUtils.ts`
  - Kept `studioTaskUtils.ts` as a compatibility re-export shim so existing `studioUtils.ts` and direct `studioTaskUtils` import paths keep working
  - Kept task title/summary, failure details, reference-image labels/counts, virtual progress, requested image count, and result-string behavior unchanged
- Phase 2 ninth chunk completed:
  - Split custom-template editor state out of `useCustomStudioTemplates.ts` into `useCustomStudioTemplateEditorState.ts`
  - Split custom-template feedback/error and edit-clear predicates into `customStudioTemplateState.ts`
  - Kept private-template behavior unchanged: draft label/title state, edit snapshot restore, template-menu focus, save completion feedback, delete cleanup, and error propagation still flow through `useCustomStudioTemplates.ts`
- Phase 2 tenth chunk completed:
  - Split reference-upload state derivation into `studioReferenceUtils.ts`: storage-path extraction, indexed removal, transient uploading tracker construction, and transient tracker cleanup checks
  - Kept reference upload behavior unchanged: base64 data URL transport, `api.uploadReferenceImage`, image filtering, size validation, preview URL release, input reset, and upload/submission tracker lifecycle remain wired through `useStudioReferenceUploads.ts`
- Phase 2 eleventh chunk completed:
  - Split history-task composer application and regenerate feedback derivation into `studioTaskActionUtils.ts`
  - Kept task reuse/regenerate behavior unchanged: task-to-form derivation, reference upload replacement, composer menu closing, scroll-back behavior, in-flight guard, pending action state, submit call, success/error feedback, and cleanup still flow through `useStudioTaskActions.ts`
- Phase 2 twelfth chunk completed:
  - Split first-pass controller wiring helpers into `studioControllerProps.ts`: selected reference-upload provider name derivation, submission-progress construction, and controller return-object assembly
  - Kept React hook call order and Studio controller behavior unchanged; `useGenerateStudioController.ts` still owns hook orchestration and only delegates pure wiring/return shaping
- Phase 2 thirteenth chunk completed:
  - Split controller hook options into focused pure helper modules: `studioControllerDataOptions.ts`, `studioControllerReferenceOptions.ts`, `studioControllerViewOptions.ts`, and `studioControllerTaskOptions.ts`, with `studioControllerHookOptions.ts` kept as an 8-line re-export shim
  - Reduced `useGenerateStudioController.ts` to 93 lines while preserving the same hook invocation order: controller state, auth, history feedback, templates, data loader, projects, reference uploads, gallery actions, view effects, task actions, submission progress, and result assembly
  - Kept behavior unchanged: the same controller state, setters, refs, derived state, selected provider fallback, upload tracker wiring, view-effect inputs, task-action inputs, and return-object shape are passed through the same hooks
- Phase 2 fourteenth chunk completed:
  - Split reference-upload list state into `useStudioReferenceUploadState.ts`
  - `useStudioReferenceUploads.ts` now stays focused on file input/drop events, base64 upload orchestration, upload tracker state, API upload calls, and input reset
  - Kept behavior unchanged: reference uploads still use base64 data URLs and `api.uploadReferenceImage`; list sync still writes `studioForm.referenceImages`; preview URLs are still released on clear/remove/replace/unmount; `replaceReferenceUploads` still replaces the local preview list without separately mutating the form because history-task reuse already sets the full form
- Phase 2 fifteenth chunk completed:
  - Split historical task-to-form derivation into `studioTaskFormUtils.ts`
  - `studioTaskActionUtils.ts` now stays focused on action-side effects: apply task to composer, composer scroll-back, and regenerate feedback derivation
  - Kept behavior unchanged: provider compatibility fallback, edit/generate mode derivation, prompt/style/aspect/resolution/deliverable/notes extraction, requested image count inference, and reference upload restoration still produce the same `nextForm` and `nextUploads`
- Phase 2 sixteenth chunk completed:
  - Split reference-upload transport into `studioReferenceUploadTransport.ts`; `studioReferenceUtils.ts` now re-exports `uploadReferenceFiles` and keeps restored-upload, preview-release, tracker, and file-preparation helpers.
  - Kept behavior unchanged: reference uploads still use base64 data URLs, `api.uploadReferenceImage`, per-file size validation, object URL previews, and failure-time preview cleanup.
  - `prepareReferenceUploadFiles` intentionally stayed in `studioReferenceUtils.ts` to avoid disturbing existing upload validation and localized error strings.
- Phase 2 seventeenth chunk completed:
  - Split custom-template save/delete React callbacks out of `useCustomStudioTemplates.ts` into `useCustomStudioTemplateMutations.ts`.
  - `useCustomStudioTemplates.ts` is now a 78-line composition hook over custom-template list state, editor state, mutation callbacks, sorted setter, and return shape.
  - Kept behavior unchanged: private-template save/delete still use the same API helper functions, draft validation, upsert/remove list updates, edit-state cleanup, feedback propagation, and global error clearing/reporting flow.
- Phase 2 eighteenth chunk completed:
  - Split task-submission state helpers into `studioTaskSubmissionState.ts`: requested-provider sync, in-flight begin guard, failure tracker/error state, and final cleanup.
  - `useStudioTaskSubmission.ts` is now 113 lines and still owns the same submit flow: close menu, clear template feedback, resolve provider, validate, build payload/tracker, call `api.createTask`, track shared-template submit success, reload data, and return success/failure.
  - Kept behavior unchanged: provider correction, duplicate-submit guard, pending/created tracker shape, failure stage, fallback error text, `hasAutoPositionedRef` reset, and `setSubmitting` cleanup remain in the same order.
- Phase 2 nineteenth chunk completed:
  - Split Studio controller return-object assembly into `studioControllerResult.ts`.
  - `studioControllerProps.ts` now re-exports `buildStudioControllerResult` for existing imports and keeps selected-reference provider-name / submission-progress helpers.
  - Kept behavior unchanged: `useGenerateStudioController.ts` still imports from `studioControllerProps.ts`, returns the same controller shape, and does not change React hook call order.
- Phase 2 twentieth chunk completed:
  - Split shared-template browser action handlers into `useSharedTemplateBrowserActions.ts`: apply tracking, quick-filter activation, category activation, and subcategory activation.
  - `useSharedTemplateBrowser.ts` is now 107 lines and still owns search/filter/category state, category availability effects, filtered-template memoization, hover preview, impressions, and return shape.
  - Kept behavior unchanged: applying a shared template still tracks the `apply` event before calling `onApplyTemplate`; quick filter/category/subcategory activation still resets the same state and category expansion behavior.
- Phase 2 twenty-first chunk completed:
  - Split the `useStudioTaskActions.ts` options/type contract into `studioTaskActionsTypes.ts`.
  - `useStudioTaskActions.ts` is now 91 lines and still owns the same hook state, submit hook wiring, apply-to-composer callback, regenerate flow, and form submit handler.
  - Kept behavior unchanged: no React hook call order changed, no API call changed, and regenerate/apply/submit feedback flow stays wired through the same helpers.
- Phase 2 twenty-second chunk completed:
  - Split the `useStudioTaskSubmission.ts` options/type contract into `studioTaskSubmissionTypes.ts`.
  - `useStudioTaskSubmission.ts` is now 96 lines and still owns the same `useCallback` submit orchestration.
  - Kept behavior unchanged: provider resolution, validation, requested-provider sync, in-flight guard, tracker creation, `api.createTask`, shared-template tracking, reload, failure state, and cleanup order are unchanged.
- Phase 2 twenty-third chunk completed:
  - Split the `useStudioGalleryActions.ts` options/type contract into `studioGalleryActionsTypes.ts`.
  - `useStudioGalleryActions.ts` is now 106 lines and still owns the same share-confirm state, bookmark/share handler, share-confirm flow, and delete-history flow.
  - Kept behavior unchanged: bookmark/share/delete API calls, pending action state, gallery preview replacement, task removal, share confirmation gating, feedback messages, and error propagation are unchanged.
- Five-stage continuation Stage 1 recheck completed at 2026-06-09 01:54 +08:00:
  - Branch remains `codex/prod-001-studio-refactor`; local HEAD remains `005e25d`
  - Working tree remains broad uncommitted WIP; no commit, push, or deploy has been performed
  - Recounted hotspots: largest TSX modules remain `StudioWorkspacePane.tsx` 52 lines and several 49-line shells; largest TS modules are `studioReferenceUtils.ts` 128 lines, `useCustomStudioTemplates.ts` 125 lines, `useStudioTaskSubmission.ts` 122 lines, `useSharedTemplateBrowser.ts` / `studioControllerProps.ts` 120 lines
  - The selected Stage 2 target was a behavior-preserving utility split in `studioReferenceUtils.ts`, separating upload transport from file-preparation/list/tracker helpers while keeping base64 upload unchanged; this was completed in the Phase 2 sixteenth chunk below.
- Five-stage continuation Stage 2/3 completed for the selected reference utility split:
  - `studioReferenceUploadTransport.ts` is now present at 42 lines and `studioReferenceUtils.ts` is down to 97 lines.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Five-stage continuation Stage 4/5 local docs refresh completed on 2026-06-09:
  - Current branch remains `codex/prod-001-studio-refactor`; local HEAD remains `005e25d`; no commit, push, or deploy was performed.
  - Rechecked current hotspots from the worktree before recording this handoff.
- Five-stage continuation Stage 1/2/3 completed for the custom-template mutation split:
  - Stage 1 selected `useCustomStudioTemplates.ts` because it was the largest hook/helper hotspot at 125 lines after the sixteenth chunk.
  - Stage 2 added `useCustomStudioTemplateMutations.ts` and kept custom-template API orchestration in `customStudioTemplateActions.ts` unchanged.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Five-stage continuation Stage 1/2/3 completed for the task-submission state split:
  - Stage 1 selected `useStudioTaskSubmission.ts` because it was the largest remaining hook hotspot at 122 lines after the seventeenth chunk.
  - Stage 2 added `studioTaskSubmissionState.ts` and kept payload construction, validation, API create-task orchestration, and shared-template event tracking in their existing helpers.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Five-stage continuation Stage 1/2/3 completed for the controller-result split:
  - Stage 1 selected `studioControllerProps.ts` because it was tied with `useSharedTemplateBrowser.ts` as the largest remaining TS hotspot at 120 lines.
  - Stage 2 added `studioControllerResult.ts`; `studioControllerProps.ts` remains the compatibility import surface for `buildStudioControllerResult`.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Five-stage continuation Stage 1/2/3 completed for the shared-template browser action split:
  - Stage 1 selected `useSharedTemplateBrowser.ts` because it was the largest remaining TS hotspot at 120 lines after the nineteenth chunk.
  - Stage 2 added `useSharedTemplateBrowserActions.ts`; existing shared-template filtering, preview, and impression helpers remain in place.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Five-stage continuation Stage 1/2/3 completed for the task-actions type-contract split:
  - Stage 1 selected `useStudioTaskActions.ts` because it was the largest remaining TS hotspot at 116 lines after the twentieth chunk.
  - Stage 2 added `studioTaskActionsTypes.ts` and moved only the cross-hook options/type contract out of `useStudioTaskActions.ts`.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Five-stage continuation Stage 1/2/3 completed for the task-submission type-contract split:
  - Stage 1 selected `useStudioTaskSubmission.ts` because it was the largest remaining TS hotspot at 113 lines after the twenty-first chunk.
  - Stage 2 added `studioTaskSubmissionTypes.ts` and moved only the submission hook options/type contract out of `useStudioTaskSubmission.ts`.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Five-stage continuation Stage 1/2/3 completed for the gallery-actions type-contract split:
  - Stage 1 selected `useStudioGalleryActions.ts` because it was the largest remaining TS hotspot at 112 lines after the twenty-second chunk.
  - Stage 2 added `studioGalleryActionsTypes.ts` and moved only the gallery actions hook options/type contract out of `useStudioGalleryActions.ts`.
  - Stage 3 verification passed again: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings.
- Current hotspot snapshot after recount:
  - Largest TSX modules: `StudioWorkspacePane.tsx` 52 lines, `StudioHistoryFeedItem.tsx` 49 lines, `StudioSharedTemplatePreviewImages.tsx` 49 lines, `StudioShareConfirmLightbox.tsx` 49 lines, `StudioComposerCollapsedBar.tsx` 47 lines, then several 46-line shells
  - Largest TS modules after the twenty-third Phase 2 chunk: `studioDerivedState.ts` / `useSharedTemplateBrowser.ts` 107 lines, `useStudioGalleryActions.ts` / `useSharedTemplatePreview.ts` 106 lines, `useStudioReferenceUploads.ts` / `useStudioTemplates.ts` 103 lines, `useCustomStudioTemplateMutations.ts` 100 lines, and `useStudioTaskSubmission.ts` 96 lines
- Verification in this checkpoint:
  - `npm run build` passed after the fourth through twelfth Phase 2 chunks
  - `npm run smoke:studio` passed locally with 8/8 checks against `http://127.0.0.1:18080` after the fourth through twelfth Phase 2 chunks
  - `git diff --check` passed with only the known CRLF warnings
  - Five-stage continuation Stage 2 browser visual smoke passed on 2026-06-09 after the Stage 1 recheck:
    - Studio route was already logged in at `http://127.0.0.1:18080/studio/generate` with title `清美道合`
    - Confirmed `.global-rail`, `.workspace-pane`, `.canvas-area`, `.composer-dock`, and `.feed-stream` render; history showed 14 feed cards, 14 feed action groups, and 4 gallery preview buttons
    - Composer expanded from the collapsed bar; textarea and toolbar buttons rendered
    - Template menu opened and rendered sidebar search, `全部 / 热度 / 最新` nav, category/subcategory nav, 6 template cards, right hover-preview container, custom-template empty section, and template editor
    - Provider menu opened with `gpt-image-2`; display menu opened with aspect-ratio and resolution options; count menu opened with 1-4 image-count options
    - Gallery lightbox opened from a safe `查看大图` action, loaded one `/media/...png` image, and closed cleanly
    - No console errors were captured during the browser checks
    - Known remaining visual risk: browser mouse-move automation still did not populate the template hover-preview content, so manual/stronger hover validation remains recommended before commit/deploy
  - Stage 3 validation after the browser smoke passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 thirteenth controller-options split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 fourteenth reference-upload state split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 fifteenth task-form split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 sixteenth reference-upload transport split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 seventeenth custom-template mutation split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 eighteenth task-submission state split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 nineteenth controller-result split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 twentieth shared-template browser action split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 twenty-first task-actions type-contract split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 twenty-second task-submission type-contract split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
  - Stage 3 validation after the Phase 2 twenty-third gallery-actions type-contract split passed: `npm run build`, `npm run smoke:studio` (8/8 against `http://127.0.0.1:18080`), and `git diff --check` with only known CRLF warnings
- Not changed:
  - no intended business behavior changes in this checkpoint
  - no Anthropic runtime adapter work
  - no real upstream model/API smoke test
  - no upload transport change; image upload remains base64
  - no deployment work
- Current risk:
  - broad frontend WIP still has many untracked files; stage intentionally and exclude `tmp/`, `.env`, `backend/app.db`, `storage/`, `frontend/dist/`, and `node_modules/`
  - behavior preservation still needs build/smoke/browser verification after each Phase 2 chunk
- Safe next step:
  - Continue Phase 2 only with a small helper split in `studioDerivedState.ts`, `useSharedTemplateBrowser.ts`, `useSharedTemplatePreview.ts`, or `useCustomStudioTemplateMutations.ts`, or pause splitting and begin final WIP review/staging preparation; avoid further controller hook-order changes now that `useGenerateStudioController.ts` is down to 93 lines
  - After the chunk, run `npm run build`, `npm run smoke:studio`, and `git diff --check`, then update `docs/handoff.md`, `docs/continuity.md`, and `docs/tasks.md`
- Safe to hand off: WIP, not yet committed

### [2026-06-08 18:20] WIP Session Handoff
- Role: Studio hotspot refactor, continuing `prod-001`
- Branch: `main`
- Repo status:
  - Working tree clean: No
  - Current local HEAD: `005e25d`
  - WIP has not been committed, pushed, or deployed
  - Tracked changes include docs, `AuthGuard`, `GenerateStudioShell`, `StudioComposerDock`, `vite.config.ts`, and `package.json`
  - New untracked Studio refactor files include `StudioAuthenticatedShell`, `StudioDesignerView`, `StudioCanvasView`, `StudioHistoryCanvas`, `StudioComposerCanvas`, `StudioComposerExpandedContent`, `StudioComposerLeading`, `StudioComposerToolbar`, `StudioComposerToolbarMenus`, `StudioComposerBody`, `StudioComposerCollapsedBar`, `StudioComposerCountMenu`, `StudioComposerDisplayMenu`, `StudioComposerProviderMenu`, `StudioComposerSubmitActions`, `StudioCustomTemplateSection`, `StudioFeedCard`, `StudioFeedCardActions`, `StudioFeedCardFooter`, `StudioAssetTile`, `StudioGalleryPreviewLightbox`, `StudioGlobalRail`, `StudioGlobalRailFooter`, `StudioGlobalRailNav`, `StudioHistoryEmptyState`, `StudioHistoryFeed`, `StudioHistoryFilters`, `StudioLoginView`, `StudioMediaLightboxes`, `StudioNewProjectForm`, `StudioShareConfirmLightbox`, `StudioSharedTemplateBrowser`, `StudioSharedTemplateGrid`, `StudioSharedTemplatePreview`, `StudioSharedTemplateSidebar`, `StudioSharedTemplateSection`, `StudioTemplateEditor`, `StudioTemplateMenu`, `StudioTemplateMenuTrigger`, `StudioWorkspaceHeader`, `StudioWorkspaceProjectActions`, `StudioWorkspaceProjectItem`, `StudioWorkspaceProjectList`, `StudioWorkspaceProjectRenameForm`, `customStudioTemplateUtils`, `modelAdminUtils`, `studioAccessUtils`, `studioComposerCanvasTypes`, `studioComposerDockTypes`, `studioComposerDockUtils`, `studioComposerExpandedContentProps`, `studioComposerExpandedContentTypes`, `studioComposerToolbarTypes`, `studioConstants`, `studioDerivedState`, `studioFeedCardTypes`, `studioGlobalRailTypes`, `studioHistoryCanvasProps`, `studioHistoryCanvasTypes`, `studioHistoryPaneTypes`, `studioMediaLightboxTypes`, `studioSubmissionProgress`, `studioTemplateBrowserUtils`, `studioTemplateMenuTypes`, `studioTemplateUtils`, `studioWorkspacePaneTypes`, `studioAssetUtils`, `studioCapabilityUtils`, `studioFormatUtils`, `studioPayloadUtils`, `studioReferenceUtils`, `studioTaskUtils`, `studioTaskActionUtils`, `studioTaskSubmissionValidation`, `studioTemplateFormUtils`, `studioTypes`, `studioUtils`, `useGenerateStudioController`, `useSharedTemplateBrowser`, `useSharedTemplatePreview`, `useSharedTemplateTracking`, `useCustomStudioTemplates`, `useStudioAuth`, `useStudioComposerCollapse`, `useStudioControllerState`, `useStudioDataLoader`, `useStudioDefaults`, `useStudioFeedCardState`, `useStudioGalleryActions`, `useStudioGalleryPreviewEffects`, `useStudioHistoryFeedback`, `useStudioProjects`, `useStudioReferenceUploads`, `useStudioTaskActions`, `useStudioTaskSubmission`, `useStudioTemplates`, `useStudioViewEffects`, and `useVirtualTaskProgress`
  - Latest sidebar split also adds `StudioSharedTemplateNav`, `StudioSharedTemplateQuickFilters`, `StudioSharedTemplateCategoryGroup`, and `StudioSharedTemplateSearch`
  - Latest toolbar menu split also adds `StudioComposerTemplateMenuSlot`, `StudioComposerProviderMenuSlot`, `StudioComposerDisplayMenuSlot`, and `StudioComposerCountMenuSlot`
  - Latest feed-action split also adds `StudioFeedActionButton` and `studioFeedActionUtils`
  - Latest shared-template preview split also adds `StudioSharedTemplatePreviewContent`, `StudioSharedTemplatePreviewImages`, `StudioSharedTemplatePreviewFallback`, `StudioSharedTemplatePreviewPlaceholder`, and `studioSharedTemplatePreviewTypes`
  - Latest feed-card header split also adds `StudioFeedCardAvatar`, `StudioFeedCardTopline`, `StudioFeedCardSummary`, `StudioFeedCardFailureDetails`, and `StudioFeedCardMeta`
  - Latest composer-body split also adds `StudioComposerModeSwitch`, `StudioReferenceDropzone`, `StudioReferenceUploadList`, and `StudioPromptTextarea`
  - Latest authenticated-shell split also adds `studioAuthenticatedShellProps`
  - Latest composer-display-menu split also adds `StudioComposerDisplayMenuPanel`, `StudioComposerDisplayMenuTrigger`, `StudioComposerOptionGroup`, and `studioComposerDisplayMenuTypes`
  - Latest feed-card-actions split moved action item derivation into `studioFeedActionUtils` and expanded `studioFeedCardTypes`
  - Latest feed-card split also adds `StudioFeedCardResult` and `studioFeedCardProps`
  - Latest custom-template-section split also adds `StudioCustomTemplateListItem` and `studioCustomTemplateSectionTypes`
  - Latest template-editor split also adds `StudioTemplateEditorHeader`, `StudioTemplateEditorFeedback`, `StudioTemplateEditorFields`, `StudioTemplateEditorActions`, and `studioTemplateEditorTypes`
  - Latest composer-dock split also adds `studioComposerDockProps` and moves collapsed-bar prop typing to `studioComposerExpandedContentTypes`
  - Latest template-menu split also adds `StudioTemplateMenuPanel` and `studioTemplateMenuProps`
  - Latest shared-template-grid split also adds `StudioSharedTemplateGridCard` and `studioSharedTemplateGridTypes`
  - Latest workspace-project split also adds `studioWorkspaceProjectTypes` and `studioWorkspaceProjectUtils`
  - Latest workspace-pane split also adds `StudioWorkspaceCreateProjectPanel`
  - Latest composer-provider-menu split also adds `StudioComposerProviderMenuTrigger`, `StudioComposerProviderMenuPanel`, `StudioComposerProviderGroup`, and `studioComposerProviderMenuTypes`
  - Latest history-filter split also adds `StudioHistoryFilterSelect` and `studioHistoryFilterOptions`
  - Latest shared-template-category split also adds `StudioSharedTemplateCategoryButton`, `StudioSharedTemplateSubcategoryList`, and `studioSharedTemplateCategoryTypes`
  - Local-only residuals still expected: `storage/`, `tmp/`
- What is true now:
  - `GenerateStudioShell.tsx` is a 24-line auth/login/authenticated-shell entrypoint.
  - The old Studio-owned admin branch has been removed from this WIP path; real admin pages live under `frontend/src/pages/admin/*` and route through `frontend/src/router.tsx`.
  - `StudioCanvasView` is now a thin composition layer over `StudioHistoryCanvas` and `StudioComposerCanvas`.
  - Shared-template category/search/sort helpers moved from `useSharedTemplateBrowser` to `studioTemplateBrowserUtils`.
  - `StudioFeedCard` now delegates asset tile rendering, footer actions, and virtual progress timing to `StudioAssetTile`, `StudioFeedCardActions`, and `useVirtualTaskProgress`; feed action button rendering and labels now live in `StudioFeedActionButton` and `studioFeedActionUtils`.
  - `StudioComposerDock` now delegates leading status and toolbar menu composition to `StudioComposerLeading` and `StudioComposerToolbar`.
  - `studioUtils.ts` is now a 7-line re-export shim over focused format/asset/task/payload/template/capability/reference utility modules.
  - `useStudioTaskActions` now delegates history-task form derivation/runtime provider resolution/composer scroll-back to `studioTaskActionUtils`, and submit preflight validation to `studioTaskSubmissionValidation`.
  - `useSharedTemplateBrowser` now delegates hover preview state/image aspect probing to `useSharedTemplatePreview`, and impression/apply/hover-preview tracking to `useSharedTemplateTracking`.
  - `useStudioTemplates` now delegates private-template draft/edit/save/delete persistence to `useCustomStudioTemplates`.
  - `useStudioTaskActions` now delegates provider validation, task payload creation, create-task calls, submission progress, and shared-template submit-success tracking to `useStudioTaskSubmission`.
  - `useCustomStudioTemplates` now delegates private-template save validation, create/update payload building, and list upsert/remove helpers to `customStudioTemplateUtils`.
  - `useStudioReferenceUploads` now delegates reference-file filtering, restored-upload creation, and preview URL release helpers to `studioReferenceUtils`; upload transport remains base64.
  - `useStudioViewEffects` now delegates composer auto-collapse/menu outside-click behavior to `useStudioComposerCollapse`, gallery lightbox effects to `useStudioGalleryPreviewEffects`, and default template/provider/project selection to `useStudioDefaults`.
  - `useGenerateStudioController` now delegates base UI state/refs/load-error helpers to `useStudioControllerState`.
  - `StudioComposerDock` is now a 63-line form shell over `StudioComposerCollapsedBar` and `StudioComposerExpandedContent`; composer dock prop types and derived labels/hints live in `studioComposerDockTypes` and `studioComposerDockUtils`.
  - `StudioSharedTemplateBrowser` now delegates sidebar/category navigation, center template grid, and right hover preview to `StudioSharedTemplateSidebar`, `StudioSharedTemplateGrid`, and `StudioSharedTemplatePreview`.
  - `StudioSharedTemplateSidebar` is now a 20-line sidebar shell; search, quick filters, category groups, and nav composition live in `StudioSharedTemplateSearch`, `StudioSharedTemplateQuickFilters`, `StudioSharedTemplateCategoryGroup`, and `StudioSharedTemplateNav`.
  - `StudioSharedTemplateCategoryGroup` is now a 30-line grouping shell; category button rendering, subcategory list rendering, and prop contracts live in focused category modules.
  - `StudioSharedTemplatePreview` is now a 27-line hover-preview shell; preview content, image rendering, fallback state, placeholder state, and hovered-template type aliases live in focused preview modules.
  - `StudioComposerCanvas` is now a 10-line visibility guard and `StudioComposerDock` pass-through; its large prop type lives in `studioComposerCanvasTypes`.
  - `StudioWorkspacePane` now delegates workspace header, new-project controls, and project list/rename UI to `StudioWorkspaceHeader`, `StudioWorkspaceCreateProjectPanel`, and `StudioWorkspaceProjectList`; pane prop types live in `studioWorkspacePaneTypes`.
  - `StudioComposerToolbar` now delegates template/provider/display/count menus to a 17-line `StudioComposerToolbarMenus` composition shell; menu prop partitioning lives in `StudioComposerTemplateMenuSlot`, `StudioComposerProviderMenuSlot`, `StudioComposerDisplayMenuSlot`, and `StudioComposerCountMenuSlot`.
  - `StudioComposerProviderMenu` is now a 31-line composition shell; trigger, panel, grouped provider rows, and prop contracts live in focused provider-menu modules.
  - `StudioWorkspaceProjectList` now delegates project item rendering, rename form, and row actions to `StudioWorkspaceProjectItem`, `StudioWorkspaceProjectRenameForm`, and `StudioWorkspaceProjectActions`.
  - `StudioWorkspaceProjectList` is now a 35-line mapping shell and `StudioWorkspaceProjectItem` is now a 42-line row renderer; workspace project prop contracts and rename key handling live in `studioWorkspaceProjectTypes` and `studioWorkspaceProjectUtils`.
  - `StudioTemplateMenu` now delegates trigger, shared-template section, custom-template list, and editor to `StudioTemplateMenuTrigger`, `StudioSharedTemplateSection`, `StudioCustomTemplateSection`, and `StudioTemplateEditor`; menu prop types live in `studioTemplateMenuTypes`.
  - `StudioComposerExpandedContent` is now a 23-line expanded composer layout shell; its prop contract and prop partitioning live in `studioComposerExpandedContentTypes` and `studioComposerExpandedContentProps`.
  - `StudioFeedCard` is now a 73-line card composition shell; footer rendering, feed-card prop types, and card derived state live in `StudioFeedCardFooter`, `studioFeedCardTypes`, and `useStudioFeedCardState`.
  - `StudioFeedCardHeader` is now a 56-line header layout shell; avatar, title/status, summary, failure details, and meta rendering live in focused feed-card header components.
  - `StudioGlobalRail` is now a 24-line global rail composition shell; navigation items, footer account/health rendering, and rail prop types live in `StudioGlobalRailNav`, `StudioGlobalRailFooter`, and `studioGlobalRailTypes`.
  - `StudioHistoryPane` is now a 38-line history pane composition shell; history filters, empty states, and filter/notice prop types live in `StudioHistoryFilters`, `StudioHistoryEmptyState`, and `studioHistoryPaneTypes`.
  - `StudioHistoryFilters` is now a 40-line layout shell over `StudioHistoryFilterSelect`; static sort/status options and provider-option derivation live in `studioHistoryFilterOptions`.
  - `StudioHistoryCanvas` is now a 19-line history canvas composition shell; history canvas props and pane/feed prop partitioning live in `studioHistoryCanvasTypes` and `studioHistoryCanvasProps`.
  - `StudioMediaLightboxes` is now a 35-line lightbox composition shell; generated-image preview and share-confirm modals live in `StudioGalleryPreviewLightbox` and `StudioShareConfirmLightbox`, with shared prop contracts in `studioMediaLightboxTypes`.
  - `StudioHistoryFeed` is now a 48-line mapping shell over `StudioHistoryFeedItem` and `studioHistoryFeedTypes`.
  - `StudioComposerBody` is now a 31-line composition shell; creation mode switching, reference-image dropzone, reference upload chips, and prompt textarea live in `StudioComposerModeSwitch`, `StudioReferenceDropzone`, `StudioReferenceUploadList`, and `StudioPromptTextarea`.
  - `StudioAuthenticatedShell` is now a 23-line layout shell; global-rail, designer-view, and lightbox prop partitioning lives in `studioAuthenticatedShellProps`.
  - `StudioComposerDisplayMenu` is now a 16-line composition shell; trigger, display panel, reusable option group, and display-menu prop types live in focused modules.
  - `StudioFeedCardActions` is now a 40-line button-group renderer over `buildStudioFeedActionItems`.
  - `StudioFeedCard` is now a 24-line composition shell over reference badge, header, result, and footer; feed-card prop partitioning lives in `studioFeedCardProps`.
  - `StudioCustomTemplateSection` is now a 32-line list-section shell; list item rendering and section/list item prop contracts live in `StudioCustomTemplateListItem` and `studioCustomTemplateSectionTypes`.
  - `StudioTemplateEditor` is now a 33-line form shell; editor header, feedback, fields, actions, and prop contracts live in focused modules.
  - `StudioComposerDock` is now a 43-line form shell; collapsed/expanded content prop partitioning lives in `studioComposerDockProps`.
  - `StudioTemplateMenu` is now a 13-line menu shell; template panel rendering and trigger/panel prop partitioning live in `StudioTemplateMenuPanel` and `studioTemplateMenuProps`.
  - `StudioSharedTemplateGrid` is now a 29-line grid shell; template card rendering and grid/card prop contracts live in `StudioSharedTemplateGridCard` and `studioSharedTemplateGridTypes`.
  - Current largest Studio TSX hotspots are about `StudioWorkspacePane` 55 lines, `StudioGlobalRailNav` 53 lines, `StudioSharedTemplatePreviewImages` 52 lines, `StudioHistoryFeedItem` 51 lines, `StudioShareConfirmLightbox` / `StudioComposerDock` 50 lines, and `StudioLoginView` / `StudioFeedGallery` / `StudioDesignerView` 49 lines.
  - Current largest Studio TS hotspots are about `useStudioReferenceUploads` 176 lines, `useCustomStudioTemplates` 171 lines, `useGenerateStudioController` 165 lines, `studioCanvasProps` 150 lines, and `useStudioTaskSubmission` 150 lines.
- Verification in this continuation:
  - `npm run build` passed after the latest `StudioSharedTemplateSidebar` split.
  - `npm run smoke:studio` passed locally with 8/8 checks against `http://127.0.0.1:18080`.
  - Browser smoke against `http://127.0.0.1:18080/studio/generate` confirmed `.global-rail` renders with Studio nav items `灵感 / 反馈 / 生成 / 对话`, `生成` is active, footer account/action text renders normally, and no console errors were logged.
  - Browser smoke also confirmed the history filter topbar renders three selects with labels `时间 / 生成类型 / 操作状态`, expected options render normally, `.feed-stream` exists, and no console errors were logged.
  - Browser smoke after the `StudioHistoryCanvas` split confirmed the history scroll container, filter topbar, feed stream, and 14 feed cards render, and no console errors were logged.
  - Browser smoke after the `StudioMediaLightboxes` split opened a generated image lightbox from a feed card, confirmed the preview image and `填入创作框 / 关闭` buttons render, closed the modal, and logged no console errors.
  - Browser smoke against `http://127.0.0.1:18080/studio/generate` confirmed the Studio shell renders, the template menu opens, and shared templates, custom templates, editor, and hover preview container are present; only React Router future-flag warnings were logged.
  - Browser smoke after the `StudioSharedTemplateSidebar` split confirmed the template menu opens, `.template-browser-sidebar` renders, search placeholder is `搜索`, quick filters show `全部 / 热度 / 最新`, category/subcategory navigation renders, right preview container exists, and no console errors were logged.
  - Browser smoke after the `StudioComposerToolbarMenus` split confirmed all four composer menu triggers render; template, provider, display, and count panels open with expected panel classes and no console errors.
  - Browser smoke after the `StudioFeedCardActions` split confirmed 14 feed action groups render with `再次生成 / 标记 / 分享 / 删除` button labels, expected disabled states, and no console errors.
  - Browser smoke after the `StudioSharedTemplatePreview` split confirmed the template menu opens, template cards and `.template-hover-preview` render, and no console errors were logged. Browser mouse-move automation did not trigger template-card hover, so manual/stronger hover validation is still recommended before commit/deploy.
  - Browser smoke after the `StudioFeedCardHeader` split confirmed 14 feed-card headers render; avatar, status pill, summary preview, failure-details summary, and meta chips render on the first card, and no console errors were logged.
  - After the `StudioComposerBody` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioAuthenticatedShell` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioComposerDisplayMenu` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioFeedCardActions` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioFeedCard` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioCustomTemplateSection` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioTemplateEditor` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioComposerDock` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioTemplateMenu` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioSharedTemplateGrid` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the workspace-project list/item prop-contract split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioWorkspaceCreateProjectPanel` split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - After the `StudioComposerProviderMenu` trigger/panel/group split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - Browser smoke after the provider-menu split expanded the collapsed composer, opened the provider menu, confirmed one provider panel with group `其他真实模型`, confirmed `gpt-image-2` remained active, and logged no console errors.
  - After the `StudioHistoryFilters` select/options split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - Browser smoke after the history-filter split confirmed one `.canvas-topbar-history`, three filter fields (`时间`, `生成类型`, `操作状态`), expected sort/provider/status options, and no console errors.
  - After the `StudioSharedTemplateCategoryGroup` button/subcategory split, `npm run build` passed, `npm run smoke:studio` passed 8/8 locally against `http://127.0.0.1:18080`, and `git diff --check` passed with only CRLF warnings.
  - Browser smoke after the shared-template-category split opened the template menu, confirmed one sidebar, category `未分类`, expanded icon, subcategory `其他`, search placeholder `搜索`, and no console errors.
  - `git diff --check` passed with only CRLF warnings.
  - `frontend\node_modules\.bin\tsc.cmd --noEmit -p frontend\tsconfig.json` was not rerun after the latest two splits; previous run failed only on known global type issues:
    - `ImportMeta.env` type declarations
    - PNG module declarations for brand assets
    - `DashboardPage.tsx` `UserGroupSummary` typing
    - `router.tsx` `JSX` namespace typing
- Not changed:
  - no Anthropic runtime adapter work
  - no real upstream model/API smoke test
  - no usage-based template popularity ranking
  - no upload transport change; image upload remains base64
  - no fully scripted online/server smoke runbook yet; current `smoke:studio` is a local/API smoke entrypoint with configurable base URL
- Current risk:
  - broad frontend WIP with many untracked files; commit only after intentional staging excludes `storage/`, `tmp/`, `frontend/dist/`, `node_modules/`, `.env`, and runtime DB files
  - behavior still needs visual smoke before deploy, especially Studio history, composer collapse, template browser, login, and real admin routes
- Safe next step:
  - continue `prod-001` by splitting `StudioWorkspacePane`, `StudioGlobalRailNav`, `StudioSharedTemplatePreviewImages`, `useGenerateStudioController`, `useStudioReferenceUploads`, `useCustomStudioTemplates`, or the pure-prop helpers
  - then run `npm run build`, `npm run smoke:studio`, `git diff --check`, and a browser smoke pass before any commit/deploy
- Safe to hand off: WIP, not yet committed

### [2026-06-08 17:30] WIP Session Handoff
- Role: Studio hotspot refactor, continuing `prod-001`
- Branch: `main`
- Repo status:
  - Working tree clean: No
  - Tracked change: `frontend/src/features/studio/GenerateStudioShell.tsx`
  - New untracked Studio refactor files:
    - `frontend/src/features/studio/StudioCanvasView.tsx`
    - `frontend/src/features/studio/StudioComposerBody.tsx`
    - `frontend/src/features/studio/StudioComposerCollapsedBar.tsx`
    - `frontend/src/features/studio/StudioAdminViewRouter.tsx`
    - `frontend/src/features/studio/StudioDesignerView.tsx`
    - `frontend/src/features/studio/StudioDashboardView.tsx`
    - `frontend/src/features/studio/StudioFeedCard.tsx`
    - `frontend/src/features/studio/StudioGlobalRail.tsx`
    - `frontend/src/features/studio/StudioHistoryFeed.tsx`
    - `frontend/src/features/studio/StudioLoginView.tsx`
    - `frontend/src/features/studio/StudioMediaLightboxes.tsx`
    - `frontend/src/features/studio/StudioModelDiscoveryPanel.tsx`
    - `frontend/src/features/studio/StudioModelProfileEditorPanel.tsx`
    - `frontend/src/features/studio/StudioModelProfilesPanel.tsx`
    - `frontend/src/features/studio/StudioModelsView.tsx`
    - `frontend/src/features/studio/StudioModelSupportNotes.tsx`
    - `frontend/src/features/studio/StudioProviderPresetPanel.tsx`
    - `frontend/src/features/studio/StudioSettingsView.tsx`
    - `frontend/src/features/studio/StudioSharedTemplateBrowser.tsx`
    - `frontend/src/features/studio/StudioTemplateMenu.tsx`
    - `frontend/src/features/studio/StudioUsersView.tsx`
    - `frontend/src/features/studio/dashboardMetrics.ts`
    - `frontend/src/features/studio/modelAdminUtils.ts`
    - `frontend/src/features/studio/studioAccessUtils.ts`
    - `frontend/src/features/studio/studioConstants.ts`
    - `frontend/src/features/studio/studioDerivedState.ts`
    - `frontend/src/features/studio/studioSubmissionProgress.ts`
    - `frontend/src/features/studio/studioTemplateUtils.ts`
    - `frontend/src/features/studio/studioTypes.ts`
    - `frontend/src/features/studio/studioUtils.ts`
    - `frontend/src/features/studio/useStudioAuth.ts`
    - `frontend/src/features/studio/useGenerateStudioController.ts`
    - `frontend/src/features/studio/useSharedTemplateBrowser.ts`
    - `frontend/src/features/studio/useStudioDataLoader.ts`
    - `frontend/src/features/studio/useStudioGalleryActions.ts`
    - `frontend/src/features/studio/useStudioHistoryFeedback.ts`
    - `frontend/src/features/studio/useStudioModelDiscovery.ts`
    - `frontend/src/features/studio/useStudioProviderProfiles.ts`
    - `frontend/src/features/studio/useStudioProjects.ts`
    - `frontend/src/features/studio/useStudioReferenceUploads.ts`
    - `frontend/src/features/studio/useStudioTaskActions.ts`
    - `frontend/src/features/studio/useStudioTemplates.ts`
    - `frontend/src/features/studio/useStudioUsers.ts`
    - `frontend/src/features/studio/useStudioViewEffects.ts`
  - Local-only residuals still expected: `storage/`, `tmp/`
- Current head:
  - local branch: `main`
  - local/GitHub head verified earlier this turn as `005e25d`
  - no commit/push/deploy has been performed in this WIP round
- What changed in this WIP round:
  - continued behavior-preserving split of `GenerateStudioShell.tsx`
  - extracted model admin page composition into `StudioModelsView`
  - extracted studio history card mapping into `StudioHistoryFeed`
  - extracted studio canvas composition into `StudioCanvasView`
  - extracted studio read-only derived view state into `studioDerivedState`
  - extracted history action pending state, transient feed feedback, global history notices, and timer cleanup into `useStudioHistoryFeedback`
  - extracted Studio data loading, request de-dupe, polling, dashboard date-window state, template synchronization, and last-sync tracking into `useStudioDataLoader`
  - extracted model discovery panel state, provider `/models` probing, model selection, assignment edits, and bulk import actions into `useStudioModelDiscovery`
  - extracted project workspace create/select/rename/archive UI actions into `useStudioProjects`
  - extracted session restore, login form state, login, and logout into `useStudioAuth`
  - extracted account-management draft/edit/save/deactivate/restore actions into `useStudioUsers`
  - extracted provider-profile draft/preset/edit/save/delete actions into `useStudioProviderProfiles`
  - extracted prompt template loading, active-template detection, draft/edit/save/delete feedback, and first-template apply behavior into `useStudioTemplates`
  - extracted reference image upload state, base64 upload flow, blob cleanup, input reset, and reference replacement into `useStudioReferenceUploads`
  - extracted gallery bookmark/share/delete actions and share confirmation state into `useStudioGalleryActions`
  - extracted task reuse/regenerate/submit actions and composer scroll-back behavior into `useStudioTaskActions`
  - extracted composer/view effects into `useStudioViewEffects`
  - extracted admin view selection and prop wiring into `StudioAdminViewRouter`
  - extracted designer workspace/canvas layout and studio-only prop wiring into `StudioDesignerView`
  - extracted submission progress derivation into `studioSubmissionProgress`
  - extracted top-level Studio state/data/action/effect hook orchestration into `useGenerateStudioController`
  - split `StudioComposerDock` so the composer shell no longer owns the shared template browser internals
  - extracted composer mode/reference upload/prompt body into `StudioComposerBody`
  - extracted collapsed composer summary into `StudioComposerCollapsedBar`
  - extracted template trigger/custom-template/editor composition into `StudioTemplateMenu`
  - extracted shared template browser layout into `StudioSharedTemplateBrowser`
  - extracted shared template search, category navigation, hover preview state, preview image measurement, and template event tracking into `useSharedTemplateBrowser`
  - extracted shared template category and preview sizing helpers into `studioTemplateUtils`
  - extracted model provider preset cards into `StudioProviderPresetPanel`
  - extracted static model runtime support notes into `StudioModelSupportNotes`
  - pinned Vite `root` to the frontend config directory to keep local Windows/sandbox builds from intermittently emitting an absolute `index.html` asset path
  - added `scripts/smoke-studio.mjs` plus `npm run smoke:studio` for a local scripted Studio smoke pass
  - `GenerateStudioShell.tsx` is now about `104` lines
  - largest Studio TSX modules after this split are now roughly `StudioDashboardView` 327 lines, `StudioComposerDock` 316 lines, and `StudioModelProfileEditorPanel` 277 lines
- Verification completed:
  - `npm run build` passed from repo root after the latest composer/template split
  - `npm run smoke:studio` passed locally with 8/8 checks against `http://127.0.0.1:18080`
  - `git diff --check` passed, with only existing CRLF warnings for docs and `GenerateStudioShell.tsx`
  - local browser smoke on `http://127.0.0.1:18080/studio/generate` rendered the studio shell with `.workspace-pane`, `.canvas-area.canvas-studio-layout`, and composer content present, and no console errors
  - `frontend\node_modules\.bin\tsc.cmd --noEmit -p frontend\tsconfig.json` still fails only on known global type issues:
    - `ImportMeta.env` type declarations
    - PNG module declarations for brand assets
    - `DashboardPage.tsx` `UserGroupSummary` typing
    - `router.tsx` `JSX` namespace typing
- Not changed:
  - no Anthropic runtime adapter work
  - no real upstream model/API smoke test
  - no usage-based template popularity ranking
  - no upload transport change; image upload remains base64
  - no fully scripted online/server smoke runbook yet; current `smoke:studio` is a local/API smoke entrypoint with configurable base URL
- Current risk:
  - this is a broad frontend refactor with many untracked files; commit only after final review/staging excludes `storage/`, `tmp/`, `frontend/dist/`, `node_modules/`, `.env`, and runtime DB files
  - behavior should be visually smoke-tested before deploy, especially Studio history, composer collapse, model admin, login, dashboard/users/settings views
- Safe next step:
  - continue `prod-001` by reducing the remaining shell state/prop orchestration in `GenerateStudioShell.tsx`, then run a deeper visual smoke pass
  - run `npm run build`, `git diff --check`, and the known-failing `tsc` check after each chunk
- Safe to hand off: WIP, not yet committed

## Context Cleanup Archive - 2026-06-09

- Archived the current conversation state to `docs/archive/prod-001-studio-refactor-context-cleanup-2026-06-09.md` before switching to a fresh thread.
- Reverified local state:
  - branch: `codex/prod-001-studio-refactor`
  - HEAD: `005e25d`
  - worktree: broad uncommitted Studio refactor WIP
- Current result:
  - TSX large-component splitting is effectively complete.
  - `GenerateStudioShell.tsx` is a thin auth/login/authenticated-shell entrypoint of about 24 lines.
  - Largest TSX hotspot is now `StudioWorkspacePane.tsx` at 52 lines; the next TSX files are around 49 lines.
  - Remaining hotspots are mainly TS hooks/utils around 100-107 lines, led by `studioDerivedState.ts`, `useSharedTemplateBrowser.ts`, `useStudioGalleryActions.ts`, and `useSharedTemplatePreview.ts`.
- Current recommendation:
  - Either do one or two small TS hook/helper splits, or stop splitting and move into final WIP review/staging preparation.
  - Before any commit/deploy, rerun build, local smoke, diff-check, and a stronger Studio visual smoke pass.
  - Continue excluding `storage/`, `tmp/`, `.env`, `backend/app.db`, `frontend/dist/`, and `node_modules/` from Git.

## Review Status Clarification - 2026-06-09

- User asked whether the whole project has been fully code-reviewed. Current answer: no.
- Completed so far: current `prod-001` Studio WIP review/staging-prep pass, including local state checks and the verification recorded in `docs/tasks.md` / `docs/continuity.md`.
- Known review finding still open before staging: `frontend/src/features/studio/useStudioReferenceUploadState.ts` uses an effect cleanup tied to `referenceUploads`; adding/removing another reference image can revoke a blob preview URL that is still retained in state, which may make an existing thumbnail disappear.
- Not completed yet: a systematic full-project review across backend, non-Studio frontend routes/pages, scripts/config, deployment/server runbooks, and GitHub/server state. Do not report the full-project review as complete until that pass is actually done.
- Safe next step: fix the open Studio preview-cleanup finding, or run the broader project review if the user wants that scope before staging.
